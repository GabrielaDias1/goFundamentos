// Crie duas variáveis booleanas temIngresso e temIdade. Use operadores lógicos para imprimir se a pessoa pode entrar em um evento (true/false).
package main

import "fmt"

func main() {
	var temIngresso bool = true
	var temIdade bool = true

	if temIngresso && temIdade {
		fmt.Println(true)
	} else {
		fmt.Println(false)
	}
}

/*Crie duas variáveis booleanas temIngresso e temIdade. Use operadores
lógicos para imprimir se a pessoa pode entrar em um evento (true/false).*/
