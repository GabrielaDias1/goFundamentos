package main

import "fmt"

func main() {
	// criando array, variavel, seu nome, tamanho array ja que array tem tamanho fixo e seu tipo 

	var numeros [6]int

	fmt.Printf("impressão: %v", numeros)
}

/*Declaração e impressão
Declare um array de inteiros com 6 posições. Imprima o array logo após a declaração (sem atribuir valores).*/
